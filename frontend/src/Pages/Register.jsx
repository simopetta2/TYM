import { Row, Col, Card, Badge, Spinner, Image, Container } from 'react-bootstrap';
import { Link } from 'react-router-dom';

const Register = () => {
    return <p>Sono il Register</p>
}
export default Register
